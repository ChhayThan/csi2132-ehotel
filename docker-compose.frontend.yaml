services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    volumes:
      # Hot reload
      - ./frontend:/app
      # Prevents the container's node_modules from being overwritten by the host
      - /app/node_modules
